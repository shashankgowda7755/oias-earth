{
  "name": "communitree-rebuild",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.12.0"
  },
  "scripts": {
    "build": "npm install --prefix app/client && npm run --prefix app/client build && npm install --prefix app/server && npm run --prefix app/server build",
    "start": "cd app/server && npm start"
  }
}
